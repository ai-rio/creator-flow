{
  "$schema": "https://raw.githubusercontent.com/aws/amazon-q-developer-cli/refs/heads/main/schemas/agent-v1.json",
  "name": "orchestrator",
  "description": "Central orchestrator for complex multi-system tasks requiring dynamic task decomposition and coordination of multiple specialist agents. Critical for CreatorFlow's agentic workflows.",
  "prompt": "You are the central orchestrator for CreatorFlow. Analyze complex tasks, decompose them into subtasks, delegate to appropriate specialist agents, and synthesize results. Follow the orchestrator patterns defined in .claude/agents/orchestrator-agent.md for task analysis, agent selection, and workflow coordination.",
  "tools": ["*"],
  "toolAliases": {
    "@memory/read_memory": "memory_read",
    "@memory/write_memory": "memory_write",
    "@filesystem/read_file": "file_read",
    "@filesystem/write_file": "file_write",
    "@supabase/query": "db_query",
    "@github/get_repository": "github_repo",
    "@fetch/fetch": "http_fetch",
    "@brave-search/search": "web_search"
  },
  "allowedTools": [
    "fs_read",
    "fs_write",
    "execute_bash",
    "use_aws",
    "@memory/*",
    "@filesystem/*",
    "@supabase/*",
    "@github/get_*",
    "@fetch/fetch",
    "@brave-search/search",
    "@serena/*",
    "@sqlite/*",
    "@playwright/*"
  ],
  "toolsSettings": {
    "fs_write": {
      "allowedPaths": [".amazonq/**/*"]
    },
    "execute_bash": {
      "allowedCommands": ["bun *", "npm *", "git *", "ls *", "find *", "grep *"]
    }
  },
  "resources": ["file://.amazonq/rules"],
  "hooks": {
    "agentSpawn": [
      {
        "command": "echo 'Orchestrator agent initialized - analyzing available specialist agents and MCP servers'"
      }
    ]
  },
  "useLegacyMcpJson": true
}
